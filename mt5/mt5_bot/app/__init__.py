# Application orchestration package.
#
# This package wires together every layer (config, data, indicators, learning,
# strategy/memory, news, decision, execution) and exposes one runner per mode.
# The top-level main.py simply parses arguments and calls into here.
